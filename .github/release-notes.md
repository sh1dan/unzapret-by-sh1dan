### What's Changed

* **Включен активный режим по умолчанию**: `dry_run = false`, стратегия `split-tcp` включена из коробки
* **Исправлен Discord Voice**: снята блокировка UDP при наличии исключений доменов, расширен пул портов
* **Корректные счетчики**: теперь пакеты реально перехватываются, фрагментируются и реинжектируются в активном режиме
